<% if @user.attrecords.first.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@user.attrecords.first.errors.count, "error") %>.
    </div>
    <ul>
    <% @user.attrecords.first.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>